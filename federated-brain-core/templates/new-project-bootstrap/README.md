# New Project Bootstrap

1. Copy this folder into the target repo root.
2. Rename `AGENTS.template.md` to `AGENTS.md`.
3. Fill `brain/project.yml` values.
4. Add domain files needed by the project.
5. Register overlay in `brain-core/catalog/projects.yml`.
