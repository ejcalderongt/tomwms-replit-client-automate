# 2026-06-17

## Durable Notes
- Proyecto activo: `mposbi`.
- Se analizó el flujo de carga de inventario y se concluyó que el principal cuello estaba en persistencia fila por fila en SQL/Firebase.
- Se documentó una estrategia batch con tag inline `EJC20260617_INVENTARIO_BATCH` para señalar puntos de futura optimización.
- Se implementó un primer corte de optimización SQL: carga de inventario con transacción, guardado por lote y actualización de costos por lote.
- Quedó pendiente la fase de Firebase por chunks y la capa de progreso/UI para que la carga no se sienta bloqueante.
- Preferencia del usuario: respuestas directas, técnicas y con pasos concretos.

- Se validó en vivo el primer corte de batch SQL para inventario en `mposbi`.
- Smoke test exitoso: login local OK, upload de Excel mínimo OK y `POST /index.php/inventario/stock/guardar_inventario` respondió `{"exito":1,"mensaje":"Inventario registrado correctamente."}`.
- Verificación en QA: se insertó el encabezado `codigo_inventario_enc = 97`, el detalle `codigo_producto = 2466` con `unidadmedida = UN`, `cant = 1`, `costo = 10`, y el costo del producto quedó actualizado a `10`.
- Quedó pendiente la fase de Firebase por chunks.

- Advertencia de arquitectura en `mposbi`: `P_stock_model::set_producto()` es un contrato compartido y sensible; tocar su rama Firebase puede romper flujos que dependen de `sinFirebase`, `sincronizar`, `rebaja`, `serial` y `key_firebase`.
- Regla de refactor seguro para inventario/Firebase: no modificar `set_producto()` en sitio para batch; crear un wrapper o writer nuevo y dejar el contrato viejo intacto para ventas, POS, traslados, recepciones e importación de inventario.
- Se habilitó autoalta quirurgica para importaciones de productos y talla/color: si faltan marca o linea se crean en la carga de productos; si faltan color, talla/color o precio en la carga de talla/color, el flujo los resuelve y reporta al final las altas adicionales.
- En la importacion de catalogo, el criterio quedo mas robusto: resolver primero `Marca` y luego `Linea`, y regresar un resumen amigable de registros auxiliares creados para no obligar a revisar logs.
- Aprendizaje nuevo: cuando un import ya tiene idempotencia suficiente, conviene preferir autoalta local sobre fallar por catálogos incompletos, siempre que el cambio no toque contratos compartidos sensibles.
- La carga masiva de inventario tambien debe dejar trazabilidad de movimiento: el smoke local valido insercion de `d_mov` y `d_movd` ademas del inventario, usando empresa 44 y sucursal 141.
- En el smoke de inventario se valido que el POST real usa `de_fila` y `a_fila`; eso evita falsos negativos al probar el endpoint manualmente.
- La inicializacion actual del sistema vive en `Inventario_model::reiniciar()` y borra `p_stock`, `p_stock_almacen`, `d_mov`, `d_movd` y `d_mov_almacen`; no encontre aun un borrado masivo de ventas en ese flujo.
- La anulacion de ventas existe como operacion puntual en `Venta_model::anularCuentaPorCobrar()` y flujos de venta/POS, pero no como parte del reset de inventario.
- Hallazgo nuevo: el futuro borrado controlado de ventas debe considerar `d_cotizacion_enc`, `d_cotizacion_det`, `d_cotizacion_enc_compra_bof`, `d_venta_bof`, `d_ventad_bof`, `d_ventad_serie_bof`, `d_cxc`, `d_cxc_pagos`, `d_nota_envio_bof_enc`, `d_nota_envio_bof_det`, `d_nota_envio_bof_serie`, `p_comision_enc`, `p_comision_det`, `razon_eliminacion_factura_comision`, `p_bitacora_registro`, `d_ticket_bitacora` e `inventario_bitacora`, con el criterio de borrar hijos primero y dejar historicos salvo que el ambiente se reinstale de cero.
- Se estructuró la inicialización con `purge_scope.php` + `Purge_scope_service`, agregando captcha obligatorio y una advertencia grande en UI para el usuario.
- En el smoke final, la sesión debía incluir `usuario.id`; sin eso la bitácora fallaba aunque la purga ya hubiera corrido.
- Se validó la purga de inventario con empresa 44 y sucursal 141; al cierre de la prueba SQL quedó en cero `p_stock`, `p_stock_almacen`, `d_mov` y `d_movd` para ese alcance.
- Se agregó respaldo lógico previo a la purga en `application/backups/purge/empresa_{empresa}/sucursal_{sucursal}/empresa_{empresa}_sucursal_{sucursal}_{timestamp}/`, con `manifest.json` + archivos por tabla.
- El smoke de respaldo se ejecutó en modo `solo_respaldo` y no ejecutó el borrado.

- Se implementó una purga/inicialización estructurada y extensible para el flujo de inventario/ventas, usando `application/config/purge_scope.php` como registro central de tablas por grupo y `Purge_scope_service` para resolver el alcance y el captcha.
- La UI de inicialización quedó con advertencia grande y el controller de `Stock` exige captcha + password de inventario antes de ejecutar el borrado.
- El diseño quedó preparado para ir agregando tablas nuevas en el futuro por grupo, sin reescribir el flujo base.
- Se documentó el inventario de tablas a eliminar y el criterio de borrado por grupos: hijos primero, padres después, filtrado por empresa/sucursal/ambiente, y mantener históricos por defecto salvo reinicialización total.
- Smoke test válido: se probó con sesión real que incluía `usuario.id`, captcha y password de inventario; la purga para empresa 44 / sucursal 141 dejó en cero `p_stock`, `p_stock_almacen`, `d_mov` y `d_movd`.
- Se dejó commit y push del cambio, con tag de liberación del flujo estructurado: `purge-inicializacion-estructurada-by-ejc`.

- Contexto nuevo: se cambió el foco a WMS en el brain federado local (`wms-brain`) y el repositorio de trabajo principal está en `C:\Users\yejc2\source\repos\TOMWMS`.
- Se detectó que para cambios WMS operativos conviene seguir el brain WMS y, según el caso, usar el agente operacional o el brain de DB para mantener coherencia entre UI, DAL, entity y SQL.
- Requerimiento nuevo en WMS: agregar `journal_memo` en `i_nav_ped_traslado_Enc` para soportar el segundo campo de observaciones de SAP junto con `comment`, especialmente para la interfaz MAMPA/SAP.
- Se trabajó el cambio de punta a punta en WMS para que `journal_memo` viaje entre solicitud de traslado y traslado, tocando SQL, Entity, DAL y WebAPI, y también la lectura desde Service Layer.
- Se dejó tag inline en el código para afinar después puntos sensibles de mapeo/inferencia SAP y trazabilidad del campo nuevo.

- En WMS, la columna `human_error` no debe depender del payload crudo completo: conviene parsear el error estructurado y guardar un resumen humano corto con los campos clave (`PROCESO`, `TIPO_DOC`, `NOENC`, `REF`, `DOCENTRY`, `ETAPA`, `CAUSA`, `RESULTADO`) más un mensaje breve.
- Para `TRANSAC_WMS`, el texto técnico largo puede exceder `U_Process_Result`; la regla práctica es simplificar antes de persistir para evitar truncamiento y mantener legible la bitácora.
- En los grids de ejecución/logs, los campos `Fecha` con hora deben mostrarse como fecha-hora completa y el detalle visual debe priorizar legibilidad con wrap/alto de celda mayor.
- Si el contexto del proceso ya identifica el origen funcional (`VENTA`, `AJUSTE`, `NC`, etc.), no hace falta repetir ese dato dentro del cuerpo de `error` o `human_error`; mejor dejarlo como columna separada o derivada.

- En WMS, el detalle de bitácora debe persistir contexto útil además del texto técnico: `human_error`, `level_log`, `codigo_producto` y `umbas` se resolvieron para guardarse aunque lleguen vacíos desde el caller, usando datos del mensaje/contexto cuando estén disponibles.
- El resumen humano del error ya no debe depender del payload crudo completo; la regla es parsear el error y guardar un `human_error` compacto con contexto clave, dejando el texto largo solo para diagnóstico.
- `TRANSAC_WMS_PROCESS_RESULT_MAX` quedó tratado como límite práctico de 50 caracteres para el envío a SAP, priorizando un mensaje muy concreto: `RESULTADO`, `PROCESO`, `ETAPA`, `CAUSA` y una referencia corta si cabe.
- La vista de logs debe mostrar `Fecha` con hora completa y usar wrap/alto de celda mayor para que `Error` y `HumanError` se lean sin truncarse en el grid.
