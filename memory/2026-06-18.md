# 2026-06-18

## Durable Notes
- The user wants `wms-brain` treated as a persistent workspace anchor so the WMS context does not need to be reloaded manually on every session.
- The active repo for WMS operational work is `C:\Users\yejc2\source\repos\TOMWMS`.
- The WMS brain should be the default context for operational changes, with the DB brain reserved for SQL/query/index tuning.
- A new workspace manifest was added at `wms-brain/README.md` to centralize the durable setup, suggested folders, and startup order.
# 2026-06-18

## Durable Notes
- The user wants `wms-brain` treated as a persistent workspace anchor so the WMS context does not need to be reloaded manually on every session.
- The active repo for WMS operational work is `C:\Users\yejc2\source\repos\TOMWMS`.
- The WMS brain should be the default context for operational changes, with the DB brain reserved for SQL/query/index tuning.
- A new workspace manifest was added at `wms-brain/README.md` to centralize the durable setup, suggested folders, and startup order.
- `Kpi/despacho` in the WebAPI is driven by `trans_despacho_det.fec_agr`, not `trans_despacho_enc`; the query also needs explicit aliases for shared columns like `idproductobodega` and `iddespachoenc` to avoid SQL ambiguity.
- KPI performance work for `despacho`, `picking`, and `verificacion` should be paired with DBA index scripts and documented in the DBA handoff notes, with the main script names `20260618_kpi_performance_indexes.sql` and `20260618_kpi_performance_indexes.down.sql`.
- WebAPI authentication uses TripleDES plus Base64 through `clsPublic.Encriptar`, and future password updates in the DB should follow that exact encoding path.

- The public WMS login issue on `8091` was traced to a schema mismatch in `Propietarios`; `GET /api/Propietario/GetAll` fails with `Column 'IdBodegaAreaSAP' does not belong to table.`
- `login-propietario` is still failing end-to-end on the published host, so the problem is not just a stale build.
- Local hardening was applied to the `Propietario` and portal log loaders so missing columns no longer crash the API, and `login-propietario` can surface the real backend exception when details are enabled.
- The live/publicable database appears to be missing fields expected by the current API contract, especially around `Propietarios` authentication and mapping columns.
# 2026-06-18

## Durable Notes
- The user wants `wms-brain` treated as a persistent workspace anchor so the WMS context does not need to be reloaded manually on every session.
- The active repo for WMS operational work is `C:\Users\yejc2\source\repos\TOMWMS`.
- The WMS brain should be the default context for operational changes, with the DB brain reserved for SQL/query/index tuning.
- A new workspace manifest was added at `wms-brain/README.md` to centralize the durable setup, suggested folders, and startup order.
# 2026-06-18

## Durable Notes
- The user wants `wms-brain` treated as a persistent workspace anchor so the WMS context does not need to be reloaded manually on every session.
- The active repo for WMS operational work is `C:\Users\yejc2\source\repos\TOMWMS`.
- The WMS brain should be the default context for operational changes, with the DB brain reserved for SQL/query/index tuning.
- A new workspace manifest was added at `wms-brain/README.md` to centralize the durable setup, suggested folders, and startup order.
- `Kpi/despacho` in the WebAPI is driven by `trans_despacho_det.fec_agr`, not `trans_despacho_enc`; the query also needs explicit aliases for shared columns like `idproductobodega` and `iddespachoenc` to avoid SQL ambiguity.
- KPI performance work for `despacho`, `picking`, and `verificacion` should be paired with DBA index scripts and documented in the DBA handoff notes, with the main script names `20260618_kpi_performance_indexes.sql` and `20260618_kpi_performance_indexes.down.sql`.
- WebAPI authentication uses TripleDES plus Base64 through `clsPublic.Encriptar`, and future password updates in the DB should follow that exact encoding path.

- The public WMS login issue on `8091` was traced to a schema mismatch in `Propietarios`; `GET /api/Propietario/GetAll` fails with `Column 'IdBodegaAreaSAP' does not belong to table.`
- `login-propietario` is still failing end-to-end on the published host, so the problem is not just a stale build.
- Local hardening was applied to the `Propietario` and portal log loaders so missing columns no longer crash the API, and `login-propietario` can surface the real backend exception when details are enabled.
- The live/publicable database appears to be missing fields expected by the current API contract, especially around `Propietarios` authentication and mapping columns.

## Durable Notes
- `mPos2026` now has a v2 brain under `C:\Users\yejc2\StudioProjects\mPos2026`, and `dev495` is the source branch/repo slice being treated as the source of truth for the scan.
- The v2 brain was organized around core, sync, sales, inventory, documents, navigation, data, and branch/agent files so the app can be resumed without re-scouting the repository.
- In the `dev495` scan, `MainActivity` is the entry point for login, permissions, license, and DB bootstrapping.
- `Menu` is the navigation hub, `Venta` plus `FacturaRes` handle sale capture and closure, and `ComWS` is the real synchronization hub.
- Inventory flows are centered on `InvBodega`, `Recarga`, `Exist`, `ComWSRec`, `ComWSPrec`, and `ComWSExist`.
- Document/print flows are covered by `clsDocument` and the `clsDoc*` classes.
- No live Firebase integration was found in the `dev495` scan.
- No active FEL/certification module was found in the `dev495` scan.
