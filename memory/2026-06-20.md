- `venta/factura_mpos` ya quedó operativa en MCP y carga en `http://127.0.0.1:8082/index.php/venta/factura_mpos`.
- El permiso para habilitar la anulación de factura mPos es `verPermisoModulo("venta", 3)`; si no está presente, la opción debe verse deshabilitada con la marca `(sin permiso)`.
- El flujo de anulación de `Factura_mpos` debe enviar la anulación a FEL cuando la factura tenga UUID/FEL y luego confirmar la anulación local en `D_FACTURA`, `D_FACTURAD` y `D_FACTURAP`.
- El setup idempotente `docs/venta/factura_mpos_setup.sql` crea la tabla de bitácora `D_FACTURA_MPOS_ANULACION` y el menú/permisos asociados, pero no un SP para listar facturas.
- La visibilidad del sidebar para `venta/factura_mpos` dependía además de `p_modulos_rol` en el módulo `14` (`Inventario`) para el rol `15`; en QA el admin operativo de empresa `80` es `adminimno` (`USERID = 297`).
- `venta/factura_mpos` ya quedó operativa en MCP y carga en `http://127.0.0.1:8082/index.php/venta/factura_mpos`.
- El permiso para habilitar la anulación de factura mPos es `verPermisoModulo("venta", 3)`; si no está presente, la opción debe verse deshabilitada con la marca `(sin permiso)`.
- El flujo de anulación de `Factura_mpos` debe enviar la anulación a FEL cuando la factura tenga UUID/FEL y luego confirmar la anulación local en `D_FACTURA`, `D_FACTURAD` y `D_FACTURAP`.
- El setup idempotente `docs/venta/factura_mpos_setup.sql` crea la tabla de bitácora `D_FACTURA_MPOS_ANULACION` y el menú/permisos asociados, pero no un SP para listar facturas.
- La visibilidad del sidebar para `venta/factura_mpos` dependía además de `p_modulos_rol` en el módulo `14` (`Inventario`) para el rol `15`; en QA el admin operativo de empresa `80` es `adminimno` (`USERID = 297`).
- El brain correcto para estos hallazgos es `mcp-brain` en GitHub; la sincronización de contexto de mPos quedó subida allí con el commit `1a7a176` (`docs(mcp-context): sync mpos factura annulment trace`).
- En empresa `80`, el menú de `venta/factura_mpos` termina de aparecer cuando al rol `15` se le habilitan tanto el permiso `venta / 3` como el acceso al módulo `14` (`Inventario`) vía `p_modulos_rol` y `p_menuopcion_rol`; tras el cambio conviene cerrar y volver a abrir sesión para refrescar el armado del menú.
- Regla durable: el brain de MCP es la fuente de verdad para el contexto persistente de esta conversación; el remote vigente para publicar ese brain es `neworigin`, no el viejo `origin` de Visual Studio.
- Regla durable: cuando se cierre una sesión con hallazgos importantes, guardar primero el resumen en `memory/YYYY-MM-DD.md` y en los mapas/README/AGENT del brain, y recién después empujar al remoto correcto.
- Regla durable: para incidentes de `mpos`/`factura_mpos`, siempre conservar en el brain el usuario, rol, empresa, SQL aplicado, commit/tag y el remote usado, para poder retomar el hilo tras una compacción.
- `venta/factura_mpos` ya quedó operativa en MCP y carga en `http://127.0.0.1:8082/index.php/venta/factura_mpos`.
- El permiso para habilitar la anulación de factura mPos es `verPermisoModulo("venta", 3)`; si no está presente, la opción debe verse deshabilitada con la marca `(sin permiso)`.
- El flujo de anulación de `Factura_mpos` debe enviar la anulación a FEL cuando la factura tenga UUID/FEL y luego confirmar la anulación local en `D_FACTURA`, `D_FACTURAD` y `D_FACTURAP`.
- El setup idempotente `docs/venta/factura_mpos_setup.sql` crea la tabla de bitácora `D_FACTURA_MPOS_ANULACION` y el menú/permisos asociados, pero no un SP para listar facturas.
- La visibilidad del sidebar para `venta/factura_mpos` dependía además de `p_modulos_rol` en el módulo `14` (`Inventario`) para el rol `15`; en QA el admin operativo de empresa `80` es `adminimno` (`USERID = 297`).
- El brain correcto para estos hallazgos es `mcp-brain` en GitHub; la sincronización de contexto de mPos quedó subida allí con el commit `1a7a176` (`docs(mcp-context): sync mpos factura annulment trace`).
- En empresa `80`, el menú de `venta/factura_mpos` termina de aparecer cuando al rol `15` se le habilitan tanto el permiso `venta / 3` como el acceso al módulo `14` (`Inventario`) vía `p_modulos_rol` y `p_menuopcion_rol`; tras el cambio conviene cerrar y volver a abrir sesión para refrescar el armado del menú.
