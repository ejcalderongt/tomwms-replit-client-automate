- 2026-06-23: MCP `mposbi` promotions smoke was executed and fixed; the Playwright suite under `assets/documentacion/tests/playwright` passed cleanly with `--workers=1`.
- 2026-06-23: `application/controllers/mnt/Descuento.php` now accepts `empresa` in `set_rango`/`set_producto` and normalizes dates in `promocion_vigente`.
- 2026-06-23: `application/models/mnt/P_descuento_model.php` now resolves `getPromocionesVigentes` with direct SQL and consistent date comparison.
- 2026-06-23: `assets/documentacion/tests/playwright/promociones-descuento.spec.js` was hardened to select products without active promos, target the created promo by name, and validate persistence after refresh.
- 2026-06-23: `assets/documentacion/tests/playwright/inventario-carga.spec.js` was changed to validate the inventory import flow through API multipart/procesar instead of browser navigation.
- 2026-06-23: `assets/documentacion/tests/playwright/seguimiento-vendedor.spec.js` remains a stable HTTP render smoke.
- 2026-06-23: `reserva_stock_from_mi3` en el WebAPI/WMS quedó orientado a observabilidad: cada intento escribe traza JSON en un árbol por fecha y expone `TraceToken` para correlación sin grabar en BD.
- 2026-06-23: El rechazo de reserva ahora se modela con causa humana, causa técnica, tag estable, evidencia y recomendación, para que el usuario vea qué revisar y el programador tenga el motivo exacto del fallo.

- 2026-06-23: En WMS Core se restauró el campo `observacion`/`Comentarios` en el pedido dentro de `WMS.DALCore` y `WMS.EntityCore`, porque quitarlo rompía el flujo de reserva con `Must declare the scalar variable "@observacion"`.
- 2026-06-23: También se corrigió el logger de errores para que genere `IdError` cuando llega en `0` y envíe `@IdError` en el `INSERT`, evitando otra falla al registrar incidencias.
- 2026-06-23: La validación contra QA del endpoint `POST /api/sync/salidas/mi3/insertar` ya no falla por `@observacion`; el bloqueo restante es funcional y responde `TIPO_NO_RESERVA=SIN_STOCK_APLICABLE | No hay stock disponible para el producto (ID: 93)`.
- 2026-06-23: La taxonomía de fallas de reserva en WebAPI quedó alineada con el legado `TOMIMSV4` para usar nombres y clasificaciones consistentes, incluyendo tags como `SIN_STOCK_APLICABLE`, `PRESENTACION_NO_APLICA`, `RESERVADO_POR_OTROS` y `RESERVA_NO_COMPLETADA`.
- 2026-06-23: Se documentó el contrato de fallas de reserva en `docs/ReservaStock_Fallas.md` para usarlo como base común entre legacy y .NET Core.
- 2026-06-23: La validación contra `TOMWMS_MHS_DEV` confirmó que para `PT741` en `MH03` sí existe stock en la bodega solicitada, por lo que el caso no corresponde a “sin registros en stock” sino a una condición funcional del pipeline.
- 2026-06-23: En `WMSWebAPI` se afinó `TotalSolicitado` para que refleje la cantidad real del request (`Quantity` de `lineas_Detalle`); en la prueba con `PT741` quedó en `13`.
