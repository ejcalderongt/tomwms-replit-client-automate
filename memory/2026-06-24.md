- AWS EC2 WMS PRD SQL (TOMWMS_BAGUTA_PRD): host 52.41.114.122, port 1437. SQL user: sa. Password: Admin1965!**. This was explicitly provided for durable use and should not be requested again. Access is IP-allowed/federated per user note.
- AWS EC2 WMS PRD SQL (TOMWMS_BAGUTA_PRD): host 52.41.114.122, port 1437. SQL user: sa. Password: Admin1965!**. This was explicitly provided for durable use and should not be requested again. Access is IP-allowed/federated per user note.

- HH (Android TOMHH2025) recepción: aplicado control lpPermitido para NO asignar LP cuando banderas están en OFF y permitir solo por excepción NAV (Orden de Producción / Transferencia de Ingreso / Devolución de Venta). Cambios concretos:
  - Bloque Caja Master y Pallet: ya no pisan txtNoLP si lpPermitido=false; trazas en español (MUTATION allowed / BLOCKED flags off).
  - Auto-licencia: case=6 (Get_Resoluciones_Lp...) ahora solo se ejecuta si lpPermitido=true; si no, traza “AUTO_LP_REQUEST.bloqueado: flags off”.
  - Se cerró ruta alterna “Call_From_D.” que llamaba execws(6) sin validar banderas.
  - Comentarios y mensajes de log en español, claros para equipo.
  - Rama: dev_2028_merge. Commit: “[REC_LP] Bloqueo de auto-licencia y arrastres de LP cuando banderas están en OFF; trazas en español (AUTO_LP_REQUEST.autorizado/bloqueado). Justificación: evitar que HH empuje LP con flags off y alinear con excepción NAV.”

- WS (WSHHRN ASMX): logger WmsTraceWS activo. Instrumentados métodos clave con Note():
  - Get_Resoluciones_Lp_By_IdOperador_And_IdBodega (RES_LP_BY_OP_BOD)
  - Existe_Lp (EXISTE_LP)
  - Guardar_Recepcion_S (GUARDAR_REC_S con snapshot de LPs en payload)
  - Ruta de logs: C:\TOM\Logs\wms-ws-trace-YYYYMMDD.log (AutoFlush ON).\n\n- Diagnóstico observado en logs antes de fix HH:
  - Con Prod=false y Pres=false, aún se ejecutaba case=6 (leak por Call_From_D) y se asignaba txtNoLP=J30000068.
  - Con Pres=true auto-LP, se reutilizaba J30000068 (correlativo no incrementaba). Ahora bloqueado cuando flags off; siguiente paso: amarrar incremento server-side.

- Próximos pasos durables:
  - En WS, trazar CORRELATIVO_BEFORE/AFTER en Guardar_Recepcion_S (incluyendo pIdResolucionLp) y, si aplica, exponer/usar método transaccional “AsignarNuevaLicencia” que incremente correlativo y devuelva LP asignada.
  - En HH, cuando lpPermitido=true, cambiar de “formatear local” a consumir el endpoint de asignación para garantizar correlativo.

- Probes/artefactos guardados:
  - DB probe scripts seguros con NOLOCK/timeout: dbprobe_ec2_safe.ps1, dbschema_probe.ps1, dbtest_ec2_*.ps1.
  - wms_lp_diagnostics.sql para banderas y anomalías.
  - Carpeta analizada con evidencias: C:\Users\yejc2\Downloads\logrecwms (Logcat y wms-ws-trace).
[2026-06-24 18:37 CST] Runtime memory flush
- HH Packing: implementado escaneo de SKU para implosión en frm_Packing.
  - Enter en txtPrd en modo LicenciasMixtas marca ítems en Recycler sin llamar WS.
  - Mapas de selección: imploSelQty (IdProductoBodega→cantidad) e imploSelIndex (enfoque/scroll).
  - Adapter list_adapt_lista_productos_cubic ahora resalta seleccionados y muestra “(sel:X)”.
  - aplicarCambioMixto() envía solo los seleccionados a Set_LP_Stock_Mixto; TRACE_IMPLO BUILD_PAYLOAD.
- HH Packing timeouts: sigue instrumentado TRACE_PACK para EXISTE_LP_BOD y GET_STOCK_BY_LP.
- WSHHRN ASMX: corregido BC30645 (WebMethod no permite Optional).
  - Recepcion_Implosion_Mover_Subconjunto: removido Optional pCommit; overload Dry con MessageName para pCommit=False.
  - Corregido catch dentro del loop (exIt) y trazas de ERROR_ITEM.
- Pendiente próximo: Asignar_Nueva_Licencia (consumo atómico de correlativo) y usarlo cuando pLicPlateDestino esté vacío; optimizar DAL de GET_STOCK_BY_LP (cronómetro, NOLOCK/TOP/paginación).
- AWS EC2 WMS PRD SQL (TOMWMS_BAGUTA_PRD): host 52.41.114.122, port 1437. SQL user: sa. Password: Admin1965!**. This was explicitly provided for durable use and should not be requested again. Access is IP-allowed/federated per user note.
- AWS EC2 WMS PRD SQL (TOMWMS_BAGUTA_PRD): host 52.41.114.122, port 1437. SQL user: sa. Password: Admin1965!**. This was explicitly provided for durable use and should not be requested again. Access is IP-allowed/federated per user note.

- HH (Android TOMHH2025) recepción: aplicado control lpPermitido para NO asignar LP cuando banderas están en OFF y permitir solo por excepción NAV (Orden de Producción / Transferencia de Ingreso / Devolución de Venta). Cambios concretos:
  - Bloque Caja Master y Pallet: ya no pisan txtNoLP si lpPermitido=false; trazas en español (MUTATION allowed / BLOCKED flags off).
  - Auto-licencia: case=6 (Get_Resoluciones_Lp...) ahora solo se ejecuta si lpPermitido=true; si no, traza “AUTO_LP_REQUEST.bloqueado: flags off”.
  - Se cerró ruta alterna “Call_From_D.” que llamaba execws(6) sin validar banderas.
  - Comentarios y mensajes de log en español, claros para equipo.
  - Rama: dev_2028_merge. Commit: “[REC_LP] Bloqueo de auto-licencia y arrastres de LP cuando banderas están en OFF; trazas en español (AUTO_LP_REQUEST.autorizado/bloqueado). Justificación: evitar que HH empuje LP con flags off y alinear con excepción NAV.”

- WS (WSHHRN ASMX): logger WmsTraceWS activo. Instrumentados métodos clave con Note():
  - Get_Resoluciones_Lp_By_IdOperador_And_IdBodega (RES_LP_BY_OP_BOD)
  - Existe_Lp (EXISTE_LP)
  - Guardar_Recepcion_S (GUARDAR_REC_S con snapshot de LPs en payload)
  - Ruta de logs: C:\TOM\Logs\wms-ws-trace-YYYYMMDD.log (AutoFlush ON).\n\n- Diagnóstico observado en logs antes de fix HH:
  - Con Prod=false y Pres=false, aún se ejecutaba case=6 (leak por Call_From_D) y se asignaba txtNoLP=J30000068.
  - Con Pres=true auto-LP, se reutilizaba J30000068 (correlativo no incrementaba). Ahora bloqueado cuando flags off; siguiente paso: amarrar incremento server-side.

- Próximos pasos durables:
  - En WS, trazar CORRELATIVO_BEFORE/AFTER en Guardar_Recepcion_S (incluyendo pIdResolucionLp) y, si aplica, exponer/usar método transaccional “AsignarNuevaLicencia” que incremente correlativo y devuelva LP asignada.
  - En HH, cuando lpPermitido=true, cambiar de “formatear local” a consumir el endpoint de asignación para garantizar correlativo.

- Probes/artefactos guardados:
  - DB probe scripts seguros con NOLOCK/timeout: dbprobe_ec2_safe.ps1, dbschema_probe.ps1, dbtest_ec2_*.ps1.
  - wms_lp_diagnostics.sql para banderas y anomalías.
  - Carpeta analizada con evidencias: C:\Users\yejc2\Downloads\logrecwms (Logcat y wms-ws-trace).
[2026-06-24 18:37 CST] Runtime memory flush
- HH Packing: implementado escaneo de SKU para implosión en frm_Packing.
  - Enter en txtPrd en modo LicenciasMixtas marca ítems en Recycler sin llamar WS.
  - Mapas de selección: imploSelQty (IdProductoBodega→cantidad) e imploSelIndex (enfoque/scroll).
  - Adapter list_adapt_lista_productos_cubic ahora resalta seleccionados y muestra “(sel:X)”.
  - aplicarCambioMixto() envía solo los seleccionados a Set_LP_Stock_Mixto; TRACE_IMPLO BUILD_PAYLOAD.
- HH Packing timeouts: sigue instrumentado TRACE_PACK para EXISTE_LP_BOD y GET_STOCK_BY_LP.
- WSHHRN ASMX: corregido BC30645 (WebMethod no permite Optional).
  - Recepcion_Implosion_Mover_Subconjunto: removido Optional pCommit; overload Dry con MessageName para pCommit=False.
  - Corregido catch dentro del loop (exIt) y trazas de ERROR_ITEM.
- Pendiente próximo: Asignar_Nueva_Licencia (consumo atómico de correlativo) y usarlo cuando pLicPlateDestino esté vacío; optimizar DAL de GET_STOCK_BY_LP (cronómetro, NOLOCK/TOP/paginación).
- HH Packing mixta: se separó el escaneo de SKU en un campo exclusivo (`txtSkuScan`) para no mezclarlo con `txtPrd`; `txtPrd` volvió al flujo normal.
  - Se añadió un banner superior con contador de selección en verde y flash `+1` al agregar SKU.
  - Se agregaron acciones por fila para sumar/restar y limpiar selección, incluyendo long press en `-` para borrar un SKU completo.
  - La confirmación no mueve todo el SKU automáticamente: acumula `+1/-1` por escaneo/acción y al confirmar sólo mueve la cantidad seleccionada por SKU.
- HH Packing ubicación: se corrigió el parsing de `txtUbicOr` para que valores con separadores de miles como `1,647` se limpien a `1647` antes de usar la ubicación en WS/casts.
- HH Packing blindaje de concurrencia/UI:
  - Se agregó bloqueo de re-entrada para evitar requests pisados mientras existe una petición en vuelo.
  - Se deshabilitan los campos y botones durante el procesamiento y se reactivan al terminar.
  - Se adoptó `ExDialog` como diálogo de error en Packing y se cierra en `onPause()`/`onDestroy()` o antes de abrir otro, para evitar overlays superpuestos.
  - `processExisteLp()` ahora tolera respuestas inesperadas tipo `JSONArray` y muestra un error controlado en vez de romper la pantalla.
