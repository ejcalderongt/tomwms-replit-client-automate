# 2026-06-25

- Se cargó el contexto de `mcp-brain` y `mcp-context-brain` para responder sobre costeo de producto en MCP.
- Hallazgo: el costeo de producto en MCP está estructurado como híbrido. `T_costo` y `D_costo` guardan el historial de eventos, `p_producto.costo` queda como costo vigente operativo, y los reportes de margen/costo leen el costo maestro al momento de consultar.
- Hallazgo: el brain documenta métodos de costo `ULTIMO_COSTO`, `COSTO_PROMEDIO` y `COSTO_PROMEDIO_PONDERADO`, con bitácora por empresa/sucursal/almacén/producto en `D_PRODUCTO_COSTO_ALMACEN`.
- En `mPos2026` (`dev495b`) se agregó `P_PRODUCTO.METODO_COSTO` al esquema/modelo/sync de producto; `InvRecep`, `InvTrans` y `WSEnv` ya lo consultan para decidir si se pisa `P_PRODUCTO.COSTO`, y `P_PARAMEXT.ID=197` quedó sólo como fallback legacy cuando el campo viene vacío.

- Hallazgo: en `mPos2026` (rama `dev495b`) el flujo de ingreso de mercancía entra por `Menu -> ListaInventario/ListaInventarioT -> InvRecep/InvTrans`; los cortes quirúrgicos están en `InvRecep.java`, `InvTrans.java`, `WSRec.processReglaCosto()`, `WSEnv.processCosto()` y el modelo/esquema local aún no expone `metodo_costo` en `clsClasses`/`clsP_productoObj`.
- Hallazgo: para modificar costeo sin romper sync, el punto crítico es dejar de pisar `P_PRODUCTO.COSTO` desde el ingreso y decidir si el cálculo nuevo vive en `InvRecep`/`InvTrans` mientras `T_costo` sigue siendo la bitácora histórica.

- Merge resuelto en `dev-merge-dev2026-495c` combinando `dev2026` y `dev405c` sin perder funcionalidades; se cerró con commit `e4b986fb` (`Merge branch 'dev495b' into dev-merge-dev2026-495c`) y `assembleDebug` pasó OK.
- Conflictos resueltos en el merge: se agregaron las clases internas faltantes en `clsClasses.java` para los DAOs nuevos de precuenta/sesión, se eliminó el método duplicado `msgboxui(String)` en `PBase.java` y se corrigió `rutanom` por `rutanombre` en `clsDocCuenta.java`.
# 2026-06-25

- Se cargó el contexto de `mcp-brain` y `mcp-context-brain` para responder sobre costeo de producto en MCP.
- Hallazgo: el costeo de producto en MCP está estructurado como híbrido. `T_costo` y `D_costo` guardan el historial de eventos, `p_producto.costo` queda como costo vigente operativo, y los reportes de margen/costo leen el costo maestro al momento de consultar.
- Hallazgo: el brain documenta métodos de costo `ULTIMO_COSTO`, `COSTO_PROMEDIO` y `COSTO_PROMEDIO_PONDERADO`, con bitácora por empresa/sucursal/almacén/producto en `D_PRODUCTO_COSTO_ALMACEN`.
- En `mPos2026` (`dev495b`) se agregó `P_PRODUCTO.METODO_COSTO` al esquema/modelo/sync de producto; `InvRecep`, `InvTrans` y `WSEnv` ya lo consultan para decidir si se pisa `P_PRODUCTO.COSTO`, y `P_PARAMEXT.ID=197` quedó sólo como fallback legacy cuando el campo viene vacío.

- Hallazgo: en `mPos2026` (rama `dev495b`) el flujo de ingreso de mercancía entra por `Menu -> ListaInventario/ListaInventarioT -> InvRecep/InvTrans`; los cortes quirúrgicos están en `InvRecep.java`, `InvTrans.java`, `WSRec.processReglaCosto()`, `WSEnv.processCosto()` y el modelo/esquema local aún no expone `metodo_costo` en `clsClasses`/`clsP_productoObj`.
- Hallazgo: para modificar costeo sin romper sync, el punto crítico es dejar de pisar `P_PRODUCTO.COSTO` desde el ingreso y decidir si el cálculo nuevo vive en `InvRecep`/`InvTrans` mientras `T_costo` sigue siendo la bitácora histórica.

- Merge resuelto en `dev-merge-dev2026-495c` combinando `dev2026` y `dev405c` sin perder funcionalidades; se cerró con commit `e4b986fb` (`Merge branch 'dev495b' into dev-merge-dev2026-495c`) y `assembleDebug` pasó OK.
- Conflictos resueltos en el merge: se agregaron las clases internas faltantes en `clsClasses.java` para los DAOs nuevos de precuenta/sesión, se eliminó el método duplicado `msgboxui(String)` en `PBase.java` y se corrigió `rutanom` por `rutanombre` en `clsDocCuenta.java`.
- Hallazgo: `mcp-context-brain` sí federó el subbrain `printagent`; la fuente de verdad documentada para el driver local es `C:\Users\yejc2\source\repos\PrintAgent`, mientras que el repo Azure DevOps `PrintAgentMCP` quedó como referencia externa no enlazada todavía en la federación local.
- Hallazgo: el reporte `Ventas_gerencial_detalle` fue restaurado en `dev2026` desde la rama `codex/reporte-seguimiento-vendedor`; la versión consolidada quedó mejorada para agrupar por fecha/vendedor, mostrar talla/color cuando aplique, usar grid colapsable DevExpress y respetar el rango de fechas al cargar.
- Hallazgo: se documentó explícitamente en el subbrain `printagent` que la identidad del repo es `C:\Users\yejc2\source\repos\PrintAgent`, rama `dev2026`, con remoto `origin` apuntando a `https://ejcalderon0892@dev.azure.com/ejcalderon0892/PrintAgentMCP/_git/PrintAgentMCP`, para evitar ambigüedad en futuras referencias.
- Se modeló el flujo de etiquetas ZPL para recepción: catálogo `p_tipo_etiqueta`, `IdTipoEtiqueta` en producto, cola `p_etiqueta_cola`, modal de preview en recepción y contrato futuro para PrintAgent con modo `label` y endpoints de cola/ack.
- El source editable del PrintAgent ya quedó localizado en `C:\Users\yejc2\source\repos\PrintAgent\print_agent_http.py`; la impresora activa está hardcoded como `PRINTER_NAME = "POS-80C"` y el dispatcher central es `do_print(payload)`.

- Hallazgo: `C:\Users\yejc2\source\repos\PrintAgent` sí es un repo git; `origin` apunta a `https://ejcalderon0892@dev.azure.com/ejcalderon0892/PrintAgentMCP/_git/PrintAgentMCP` y la rama de trabajo visible es `dev2026`, por lo que ya existe una fuente de referencia estable para el driver local.
- Hallazgo: el cambio de impresión ZPL quedó modelado como contrato, no como implementación del `py` todavía: catálogo `p_tipo_etiqueta` por empresa, `IdTipoEtiqueta` en `p_producto`, cola `p_etiqueta_cola` con estados de trabajo, modal de preview/confirmación en recepción y un modo `label` para PrintAgent con placeholders tipo `{producto.codigo}`, `{producto.nombre}`, `{producto.talla}`, `{producto.color}`, `{producto.codbarra}`, `{producto.sku}`, `{producto.precio}` y `{producto.logo}`.

- Hallazgo: en `PrintAgent`, el source editable quedó confirmado en `C:\Users\yejc2\source\repos\PrintAgent\print_agent_http.py`; la impresora activa está hardcoded como `PRINTER_NAME = "POS-80C"` y el punto de entrada de impresión es `do_print(payload)`, por lo que el cambio para etiquetas/ZPL debe ramificarse ahí sin romper el contrato actual de tickets.
- Hallazgo: la documentación de modificación del driver quedó asentada en el brain federado de `printagent` para evitar ambigüedad futura sobre la fuente de verdad del repo y el comportamiento del modo `label`.
- Hallazgo: se ejecutó un smoke automatizado extremo a extremo para recepción + cola + PrintAgent. Se creó tipo de etiqueta `SMOKE-ZPL-24`, se asignó al producto `1620`, se levantó una orden de compra de prueba y una recepción automatizada con factura dummy. El flujo terminó con `Recepción realizada con éxito`, `cola_etiquetas.total = 1` y una fila `PENDIENTE` en `p_etiqueta_cola`.
- Hallazgo: durante el smoke de recepción se corrigieron tres bloqueos reales del flujo: adjuntos binarios vía `OPENROWSET(BULK)` contra SQL remoto, fallback de Firebase en `P_stock_model` para no tumbar la recepción si falla SSL, y el registro de `D_PRODUCTO_COSTO_ALMACEN` para no insertar identity/campos incompatibles.
- Hallazgo: el `PrintAgent` local procesó correctamente el modo `label` con ZPL simulado usando `print_agent_http.py`; el smoke devolvió `ok=true`, `mode=label` y generó ZPL sin dejar placeholders sin resolver.
# 2026-06-25

- Se cargó el contexto de `mcp-brain` y `mcp-context-brain` para responder sobre costeo de producto en MCP.
- Hallazgo: el costeo de producto en MCP está estructurado como híbrido. `T_costo` y `D_costo` guardan el historial de eventos, `p_producto.costo` queda como costo vigente operativo, y los reportes de margen/costo leen el costo maestro al momento de consultar.
- Hallazgo: el brain documenta métodos de costo `ULTIMO_COSTO`, `COSTO_PROMEDIO` y `COSTO_PROMEDIO_PONDERADO`, con bitácora por empresa/sucursal/almacén/producto en `D_PRODUCTO_COSTO_ALMACEN`.
- En `mPos2026` (`dev495b`) se agregó `P_PRODUCTO.METODO_COSTO` al esquema/modelo/sync de producto; `InvRecep`, `InvTrans` y `WSEnv` ya lo consultan para decidir si se pisa `P_PRODUCTO.COSTO`, y `P_PARAMEXT.ID=197` quedó sólo como fallback legacy cuando el campo viene vacío.

- Hallazgo: en `mPos2026` (rama `dev495b`) el flujo de ingreso de mercancía entra por `Menu -> ListaInventario/ListaInventarioT -> InvRecep/InvTrans`; los cortes quirúrgicos están en `InvRecep.java`, `InvTrans.java`, `WSRec.processReglaCosto()`, `WSEnv.processCosto()` y el modelo/esquema local aún no expone `metodo_costo` en `clsClasses`/`clsP_productoObj`.
- Hallazgo: para modificar costeo sin romper sync, el punto crítico es dejar de pisar `P_PRODUCTO.COSTO` desde el ingreso y decidir si el cálculo nuevo vive en `InvRecep`/`InvTrans` mientras `T_costo` sigue siendo la bitácora histórica.

- Merge resuelto en `dev-merge-dev2026-495c` combinando `dev2026` y `dev405c` sin perder funcionalidades; se cerró con commit `e4b986fb` (`Merge branch 'dev495b' into dev-merge-dev2026-495c`) y `assembleDebug` pasó OK.
- Conflictos resueltos en el merge: se agregaron las clases internas faltantes en `clsClasses.java` para los DAOs nuevos de precuenta/sesión, se eliminó el método duplicado `msgboxui(String)` en `PBase.java` y se corrigió `rutanom` por `rutanombre` en `clsDocCuenta.java`.
# 2026-06-25

- Se cargó el contexto de `mcp-brain` y `mcp-context-brain` para responder sobre costeo de producto en MCP.
- Hallazgo: el costeo de producto en MCP está estructurado como híbrido. `T_costo` y `D_costo` guardan el historial de eventos, `p_producto.costo` queda como costo vigente operativo, y los reportes de margen/costo leen el costo maestro al momento de consultar.
- Hallazgo: el brain documenta métodos de costo `ULTIMO_COSTO`, `COSTO_PROMEDIO` y `COSTO_PROMEDIO_PONDERADO`, con bitácora por empresa/sucursal/almacén/producto en `D_PRODUCTO_COSTO_ALMACEN`.
- En `mPos2026` (`dev495b`) se agregó `P_PRODUCTO.METODO_COSTO` al esquema/modelo/sync de producto; `InvRecep`, `InvTrans` y `WSEnv` ya lo consultan para decidir si se pisa `P_PRODUCTO.COSTO`, y `P_PARAMEXT.ID=197` quedó sólo como fallback legacy cuando el campo viene vacío.

- Hallazgo: en `mPos2026` (rama `dev495b`) el flujo de ingreso de mercancía entra por `Menu -> ListaInventario/ListaInventarioT -> InvRecep/InvTrans`; los cortes quirúrgicos están en `InvRecep.java`, `InvTrans.java`, `WSRec.processReglaCosto()`, `WSEnv.processCosto()` y el modelo/esquema local aún no expone `metodo_costo` en `clsClasses`/`clsP_productoObj`.
- Hallazgo: para modificar costeo sin romper sync, el punto crítico es dejar de pisar `P_PRODUCTO.COSTO` desde el ingreso y decidir si el cálculo nuevo vive en `InvRecep`/`InvTrans` mientras `T_costo` sigue siendo la bitácora histórica.

- Merge resuelto en `dev-merge-dev2026-495c` combinando `dev2026` y `dev405c` sin perder funcionalidades; se cerró con commit `e4b986fb` (`Merge branch 'dev495b' into dev-merge-dev2026-495c`) y `assembleDebug` pasó OK.
- Conflictos resueltos en el merge: se agregaron las clases internas faltantes en `clsClasses.java` para los DAOs nuevos de precuenta/sesión, se eliminó el método duplicado `msgboxui(String)` en `PBase.java` y se corrigió `rutanom` por `rutanombre` en `clsDocCuenta.java`.
- Hallazgo: `mcp-context-brain` sí federó el subbrain `printagent`; la fuente de verdad documentada para el driver local es `C:\Users\yejc2\source\repos\PrintAgent`, mientras que el repo Azure DevOps `PrintAgentMCP` quedó como referencia externa no enlazada todavía en la federación local.
- Hallazgo: el reporte `Ventas_gerencial_detalle` fue restaurado en `dev2026` desde la rama `codex/reporte-seguimiento-vendedor`; la versión consolidada quedó mejorada para agrupar por fecha/vendedor, mostrar talla/color cuando aplique, usar grid colapsable DevExpress y respetar el rango de fechas al cargar.
- Hallazgo: se documentó explícitamente en el subbrain `printagent` que la identidad del repo es `C:\Users\yejc2\source\repos\PrintAgent`, rama `dev2026`, con remoto `origin` apuntando a `https://ejcalderon0892@dev.azure.com/ejcalderon0892/PrintAgentMCP/_git/PrintAgentMCP`, para evitar ambigüedad en futuras referencias.
- Se modeló el flujo de etiquetas ZPL para recepción: catálogo `p_tipo_etiqueta`, `IdTipoEtiqueta` en producto, cola `p_etiqueta_cola`, modal de preview en recepción y contrato futuro para PrintAgent con modo `label` y endpoints de cola/ack.
- El source editable del PrintAgent ya quedó localizado en `C:\Users\yejc2\source\repos\PrintAgent\print_agent_http.py`; la impresora activa está hardcoded como `PRINTER_NAME = "POS-80C"` y el dispatcher central es `do_print(payload)`.

- Hallazgo: `C:\Users\yejc2\source\repos\PrintAgent` sí es un repo git; `origin` apunta a `https://ejcalderon0892@dev.azure.com/ejcalderon0892/PrintAgentMCP/_git/PrintAgentMCP` y la rama de trabajo visible es `dev2026`, por lo que ya existe una fuente de referencia estable para el driver local.
- Hallazgo: el cambio de impresión ZPL quedó modelado como contrato, no como implementación del `py` todavía: catálogo `p_tipo_etiqueta` por empresa, `IdTipoEtiqueta` en `p_producto`, cola `p_etiqueta_cola` con estados de trabajo, modal de preview/confirmación en recepción y un modo `label` para PrintAgent con placeholders tipo `{producto.codigo}`, `{producto.nombre}`, `{producto.talla}`, `{producto.color}`, `{producto.codbarra}`, `{producto.sku}`, `{producto.precio}` y `{producto.logo}`.

- Hallazgo: en `PrintAgent`, el source editable quedó confirmado en `C:\Users\yejc2\source\repos\PrintAgent\print_agent_http.py`; la impresora activa está hardcoded como `PRINTER_NAME = "POS-80C"` y el punto de entrada de impresión es `do_print(payload)`, por lo que el cambio para etiquetas/ZPL debe ramificarse ahí sin romper el contrato actual de tickets.
- Hallazgo: la documentación de modificación del driver quedó asentada en el brain federado de `printagent` para evitar ambigüedad futura sobre la fuente de verdad del repo y el comportamiento del modo `label`.
- Hallazgo: se ejecutó un smoke automatizado extremo a extremo para recepción + cola + PrintAgent. Se creó tipo de etiqueta `SMOKE-ZPL-24`, se asignó al producto `1620`, se levantó una orden de compra de prueba y una recepción automatizada con factura dummy. El flujo terminó con `Recepción realizada con éxito`, `cola_etiquetas.total = 1` y una fila `PENDIENTE` en `p_etiqueta_cola`.
- Hallazgo: durante el smoke de recepción se corrigieron tres bloqueos reales del flujo: adjuntos binarios vía `OPENROWSET(BULK)` contra SQL remoto, fallback de Firebase en `P_stock_model` para no tumbar la recepción si falla SSL, y el registro de `D_PRODUCTO_COSTO_ALMACEN` para no insertar identity/campos incompatibles.
- Hallazgo: el `PrintAgent` local procesó correctamente el modo `label` con ZPL simulado usando `print_agent_http.py`; el smoke devolvió `ok=true`, `mode=label` y generó ZPL sin dejar placeholders sin resolver.
- Hallazgo: el source editable de `PrintAgent` quedó localizado y documentado en `C:\Users\yejc2\source\repos\PrintAgent\print_agent_http.py`; `PRINTER_NAME = "POS-80C"` sigue hardcoded como impresora activa, `do_print(payload)` es el dispatcher central y la guía de modificación quedó documentada en el brain federado con `SOURCE-MODIFICATION-GUIDE.md` y `LABEL-FLOW.md`.

- Hallazgo: se inyectó el menú de mantenimiento `Tipos de etiqueta` para validación en QA; la ruta visible debe ser `mnt/tipo_etiqueta`, con ícono `ri-price-tag-3-line`, módulo `Mantenimientos (idmodulo = 4)`, `menu_id = 225` y relaciones de rol creadas (`79`). El alta quedó documentada en `assets/documentacion/db_changes/20260625_tipo_etiqueta_menu.sql` para poder reproducirla o re-aplicarla sin depender de memoria oral.
- Se creó y activó la skill `federated-brain-bootstrap` para arrancar sesión con el núcleo de contexto federado y compilar brains/YAML/árboles relevantes.
- Se actualizó `AGENTS.md` para que el bootstrap por defecto del workspace apunte a `federated-brain-bootstrap`, dejando explícito que el contexto inicial debe salir de `MEMORY.md`, la nota diaria y el brain activo.
- Regla nueva: el bootstrap es progresivo. Cada sesión debe refinar el pack de contexto y promover a `MEMORY.md` lo que ya sea estable y recurrente.
- Se añadió `brain/federated-index.yml` como manifest único de routing y `scripts/bootstrap-session.ps1` como bootstrap ejecutable que consume ese índice para cargar el contexto mínimo por dominio.
- Se inyectó el arranque visible dentro de `wms-brain` con `wms-brain/bootstrap-session.ps1`, dejando ese brain como el anchor portátil para restaurar WMS en otra máquina o ambiente.
- El usuario aclaró que `wms-brain` vive en `https://github.com/ejcalderongt/tomwms-replit-client-automate`; dejar de preguntar por su ubicación y tratar esa URL como fuente de verdad del brain.

# 2026-06-25 - Implosion/SKU bootstrap

- Canonical GitHub home for `wms-brain` is `https://github.com/ejcalderongt/tomwms-replit-client-automate`; treat that repo as the source of truth for WMS brain backups/restores.
- The federated startup/bootstrapping structure now lives in `brain/federated-index.yml`, `scripts/bootstrap-session.ps1`, and `wms-brain/bootstrap-session.ps1`; `wms-brain` is the visible WMS anchor entry point.
- For implosion / mixed pallet SKU validation, the new flow should use `Get_BeProducto_By_Codigo_Or_Sku_For_HH` as the VW_ProductoSI-backed resolver and `Validar_Sku_Implosion_JSON` as the HH validation endpoint.
- The implosion validation contract should reject scans that exceed the selected quantity (`imploSelQty`) and surface an explicit completion message when the product/talla/color is already fully scanned.
- The HH implosion form now uses `txtSkuScan` as the scan entrypoint; the match is exact against `CodigoSKU` with no fuzzy search, and the comparison is explicitly tagged in code with `#CKFK20260625`.
- Build validation is currently blocked by pre-existing syntax corruption in `clsLnStock_Partial.vb`; the implosion form changes themselves are wired, but the DAL project must be cleaned before the full solution can compile.
- Existing implosion/Packing webservice methods still in the end-to-end path include `Get_BeProducto_By_Codigo_For_HH_JSON`, `Get_Productos_By_IdUbicacion`, `Get_Stock_By_Lic_Plate_And_Codigo`, `Get_All_Presentaciones_By_IdProducto`, `Set_LP_Stock_Mixto`, `Get_Resoluciones_Lp_By_IdOperador_And_IdBodega`, `Existe_Lp_By_Licencia_And_IdBodega_JSON`, and `Recepcion_Implosion_Mover_Subconjunto`.
- Do not ask again for the canonical `wms-brain` repo URL in future turns; it is already documented and should be treated as known context.

- Hallazgo nuevo: en `frmImplosion` ya quedó cableado `txtSkuScan` para disparar el flujo de scan/confirmación; el match debe ser exacto contra `CodigoSKU` de la lista cargada, sin fuzzy/contains, y se dejó comentario inline con tag `#CKFK20260625` señalando explícitamente dónde ocurre la comparación.
- Hallazgo nuevo: `Validar_Sku_Implosion_JSON` quedó como el punto de validación de SKU/barcode contra `VW_ProductoSI` para implosión, con corte cuando la cantidad objetivo ya está completada para poder lanzar el exdialog correspondiente.
- Decisión de arquitectura: para reducir búsquedas pesadas y mejorar trazabilidad, conviene partir el brain por proceso (`Picking`, `Packing`, `Recepcion`, `Verificacion`, `Cambio de Ubicacion`, `Cambio de Estado`) y mantener por cada uno su `flow.yml`, `ws-map.yml`, `ui-map.yml`, `trace-endtoend.yml`, `rules.yml` y `notes.md`.
- Regla de evolución: los brains/procesos deben ser progresivos; cada día se promueven hallazgos estables a memoria duradera y se afinan trazas/contratos end-to-end sin mezclar procesos distintos en un solo archivo gigante.
# 2026-06-25

- Packing/implosion finding: the scan path on `frm_Packing` was falling back from `Get_BeProducto_By_Codigo_Or_Barra_For_HH` to a local list match, and that fallback was using prefix-style matching (`startsWith`), which made `AG00028240004UNI` incorrectly resolve to the base code `AG00028240`.
- Packing/implosion fix direction: SKU validation must be exact against `CodigoSKU`, not against the base `Codigo`; the exact-match path should be explicit in code and tagged for future maintenance.
- Packing/implosion backend note: `Validar_Sku_Implosion_JSON` exists as the WS validation contract, but the UI flow still needs to consume it consistently so the HH does not degrade to the legacy fallback when the exact SKU scan should be authoritative.
- Packing/implosion data note: `CodigoSKU` must be preserved through product/stock queries so the list/grid can display and validate the same SKU that the HH scans, instead of only showing the base product code.
- Architecture note: future context retrieval for warehouse work should be split by process subbrains/YAMLs (for example `Picking`, `Packing`, `Recepcion`, `Verificacion`, `Cambio de Ubicacion`, `Cambio de Estado`) so end-to-end traces stay small, exact, and cheaper to load.

- En Packing/Implosion la lógica de scan vive en la HH Android (`frm_Packing.java` / `txtSkuScan`), no en VB; el backend solo debe exponer el contrato y no cargar la UI de matching.
- El endpoint correcto para Packing/Implosion es `Get_BeProducto_By_Codigo_Or_Sku_For_HH`, con búsqueda en `VW_ProductoSI` por código/barra/SKU; `Get_List_Product_By_CodigoBarra_By_Ciclico` quedó como ruta legacy de inventario cíclico y no debe ser el origen del flujo de Packing.
- `CodigoSKU` no debe agregarse a la entidad base `clsBeProducto`; debe resolverse en una proyección/DTO de implosión o en `producto_talla_color`, manteniendo la entidad base pura.
- El match de scan en la HH debe ser exacto por SKU y, cuando sea posible, resolver primero por IDs exactos (`IdProductoTallaColor`, `IdProductoBodega`, `IdStock`) y usar `CodigoSKU` solo como confirmación estricta, con mensajes que distingan entre "no existe en maestro" y "no coincide con la lista cargada".
- Hallazgo nuevo: el proceso de Verificación en la HH Android vive en `frm_verificacion_datos.java` y pantallas vecinas (`frm_list_prod_reemplazo_verif`, `frm_danado_verificacion`, `frm_verificacion_consolidada_detalle`); ahí `CodigoSKU` es la llave operativa cuando `Control_Talla_Color` está activo, mientras que sin talla/color se usa `Codigo` base.
- Hallazgo nuevo: Verificación valida cantidad antes de guardar, y el flujo soporta verificación consolidada, sin lote/fecha de vencimiento, reemplazos y fotos. No debe mezclarse con Packing ni con Inventario Cíclico.
- Decisión nueva: crear un subbrain federado específico para Verificación (`brain/wms/verificacion`) y cargarlo por ruta propia para no volver a arrastrar lógica de otros procesos.
- Regla de documentación: cuando un proceso HH tenga código operativo distinto según bandera, documentar explícitamente cuál es la llave real del match (`CodigoSKU` vs `Codigo`) y el método WS involucrado para que la próxima sesión no adivine.

- Packing HH: el SKU visible en la lista se deriva en `frm_Packing.java` y la convención correcta quedó confirmada como `Codigo + Color + Talla` (no `Codigo + Talla + Color`). El matcher de scan debe ser exacto y no debe caer a `IdProductoBodega` porque eso resalta variantes incorrectas.
- Packing HH: `Get_BeProducto_By_Codigo_Or_Sku_For_HH` quedó como endpoint limpio para resolver sobre `VW_ProductoSI`; el alias legacy por inventario cíclico debe evitarse para este flujo.
- Packing HH: la búsqueda local debe usar primero llaves exactas de variante (`IdStock`, `IdProductoTallaColor`) y solo usar `CodigoSKU`/barcode como respaldo exacto, con trazas `TRACE_IMPLO` para auditar qué comparó y por qué ganó.
- Verificación HH: hay un bug donde, al asignar empaque, el flujo cae en verificación consolidada y consulta una vista desactualizada (`VW_Verificacion_Consolidada_LFV`), dejando además el `progress` activo cuando falla la carga.
- Verificación HH: otro bug detectado es promoción prematura de estados; al completar picking se marca como verificado antes de tiempo, bloqueando pendientes de picking/verificación hasta forzar cambios por BD.
- Verificación HH: el fix correcto requiere instrumentar primero para separar ruta de empaque vs consolidada, asegurar cierre de `progress` en todas las ramas de error y cambiar la promoción de estado a una condición de pendientes en cero.
